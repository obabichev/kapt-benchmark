# Kotlin version (public release from Maven Central / Plugin Portal)
kotlin.version=2.3.21

# Annotation processor versions
dagger.version=2.59.2
mapstruct.version=1.6.3

# Source generator settings
generatorSeed=42

# Gradle settings
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.configuration-cache=false
